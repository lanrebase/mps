<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC     
    "-//mybatis.org//DTD Mapper 3.0//EN"    
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kuyun.mps.event.mapper.EventMapper">
    <select id="getCount" resultType="java.lang.Integer">
        select count(1) from mps.t_event
    </select>
    <select id="getEventList"  parameterType="Map"  resultType="TEvent">
        select * from mps.t_event order by app_code limit #{startNo},#{endNo}
    </select>
    <insert id="saveEvnet" parameterType="TEvent">
    	 insert into mps.t_event(
    	 	event_id,
    	 	app_code,
    	 	event_name,
    	 	event_desc,
    	 	op_type,
    	 	score
    	 ) 
    	 values(
    	 	#{event_id},
    	 	#{app_code},
    	 	#{event_name},
    	 	#{event_desc},
    	 	#{op_type},
    	 	#{score}
    	 )
    </insert>
    
</mapper>